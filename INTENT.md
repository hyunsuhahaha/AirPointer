# Intent

이 문서는 "무엇을 만들었나"(README.md)나 "누구를 위한 것인가"(PRODUCT.md)가 아니라,
**왜 이런 구조로 결정했는가**를 적어둔다. 코드만 봐서는 안 드러나는 트레이드오프와, 다음에
누가(사람이든 AI든) 이어받아 작업할 때 같은 논쟁을 반복하지 않도록 하기 위한 기록이다.

## 최근 우선순위: 무설치 브라우저 체험

대회 운영진의 제출 기준은 심사위원이 별도 설치 없이 핵심 기능을 확인할 수 있는가이다.
검증되지 않은 실행파일 다운로드를 필수로 요구할 수 없으므로 **대회 제출·공개 체험의
우선 경로는 순수 브라우저 + Document PiP**다. 아래 네이티브 중심 원칙은 Windows 확장
기능에 대한 기존 설계이며, 브라우저 체험의 기능 확장을 막는 원칙이 아니다.

브라우저는 화면 공유, 메모리 순환 버퍼, 변화 감지, 미리보기 영역 선택과 서버 AI 분석을
독립적으로 제공한다. PiP는 버퍼 상태·변화 전후·캡처·영역 선택·대화를 한 창에 제공한다.
질문만 보내기는 새 화면을 첨부하지 않으며 이전 대화 텍스트만 사용한다. PiP는 React
portal로 같은 상태와 요청 잠금을 공유한다. OS 전역 단축키나 접근성 트리 읽기는 네이티브에 남는다.

## 핵심 원칙

- **네이티브 앱이 중심, 브라우저는 얇은 리모컨이다.** 실제 로직(화면 캡처, 변화 감지,
  전송)은 `airpointer/`에 있다. `web/`은 이 로직을 다시 구현하는 곳이 아니라, 그걸 켜고 끄고
  설정을 조정하는 원격 컨트롤이어야 한다. (예외: 웹은 브라우저 자체 화면공유로 별도 캡처
  경로도 갖고 있다 — 아래 "알려진 중복" 참고. 이건 원칙 위반이 아니라 웹 데모가 설치 없이도
  동작해야 한다는 별도 요구 때문에 생긴 것.)
- **단축키 모드가 주력, 제스처(카메라) 모드는 보조.** 실사용에서 제스처 모드는 거의 안
  쓰인다 — 카메라를 켜야 하고, 손 인식 지연이 있고, 조명에 민감하다. 새 기능을 만들 때
  "제스처 쪽도 똑같이 되나"를 먼저 걱정하지 말 것. 단축키 경로가 먼저 완성되고 검증되면
  충분하고, 제스처 쪽 패리티는 나중 우선순위다.
- **모드는 켜기 전에 고르는 것이지, 켜진 채로 얹는 토글이 아니다.** 시작 모드(제스처/단축키)
  선택은 AirPointer를 켜기 전에 한 번 확정한다 — 실행 중 전환 가능한 옵션으로 만들지 않는다.
  두 모드는 "같이 켤 수 있는 기능"이 아니라 "둘 중 하나"다.
- **일회성 로컬 리플레이지, 상시 녹화가 아니다.** 화면은 짧은 순환 버퍼에만 남고, 명시적
  트리거(제스처/단축키) 없이는 아무 데도 전송되거나 영구 저장되지 않는다. 이건 Windows
  Recall류(평문 저장, 통째 유출 가능)가 겪은 실패를 피하기 위한 핵심 설계다
  (`docs/replay-change-detection.md`의 "관련 연구" 절 참고).

## 프레임 선택: "많이 바뀐 것"과 "중요한 것"은 다른 축이다

리플레이 전송은 항상 프레임 개수 제한(기본 6장)이 있다. 처음엔 diff score(화면이 얼마나
바뀌었나) 순으로 상위 N개를 그냥 잘랐는데, 실사용 시나리오를 생각해보면 문제가 분명했다:
스크롤 3번이 에러 토스트 1번보다 항상 "더 크게" 바뀐 걸로 측정되므로, 정작 중요한 작은
변화가 큰 변화들에 밀려 탈락한다.

그래서 지금은 두 단계로 나눈다:

1. **MMR(다양성 재순위)**: 점수는 높지만 이미 뽑힌 것과 겹치는 이벤트는 점수를 깎아서,
   비슷한 큰 변화가 슬롯을 독점하지 못하게 함.
2. **국소 이벤트 최저 보장(floor)**: 그래도 작고 국소적인 변화(토스트류)가 하나도 안
   뽑혔으면 강제로 하나 끼워 넣음.

(`airpointer/screen_buffer.py`의 `_select_events_diverse`, 설계 근거는
`docs/replay-change-detection.md`)

그래도 여전히 놓치는 경우가 있을 수 있다는 걸 인정하고, **선택을 없애는 대신 얹는** 방식을
택했다: 6장은 그대로 빠르게 먼저 보내고(대부분의 질문은 이걸로 충분, 추가 비용 0), 모자라면
Codex가 스스로 원본을 다시 조회할 수 있는 매니페스트를 같이 준다(`airpointer/replay_query.py`).
"선택 알고리즘을 없애고 원본을 통째로 주자"는 대안도 검토했지만 기각했다 — Codex는 영상을
직접 보는 도구가 없어서, 미리 뽑아주지 않으면 제일 단순한 질문에도 매번 추가 도구 호출이
필요해지기 때문이다. 두 경로를 같이 두는 게 하나만 있는 것의 상위 호환이다.

`_LOCALIZED_EXTENT_MAX`, `_REDUNDANCY_LAMBDA`, 프레임 개수 6 같은 구체적 숫자들은 전부
**실측 튜닝이 아니라 합리적 출발점**이다 — 나중에 실사용 패턴을 보고 바뀔 수 있다.

## 전송 메커니즘: 붙여넣기가 유일하게 신뢰 가능한 경로다

`airpointer/codex_delivery.py`에 `CodexAppServerDelivery`(별도 `codex app-server --stdio`
서브프로세스를 띄워 API로 스레드에 "붙는" 방식)가 정의는 돼 있지만, **네이티브 앱은 이걸
실제로 인스턴스화하지 않는다.** 이유: Codex Desktop 앱이 이미 그 스레드를 열어놓고 있으면
그 자체가 "active writer"라서 락 충돌·크래시가 난다 — 재시도 로직으로 고칠 수 있는 버그가
아니라 "같은 대화에 쓰는 프로세스가 둘이면 안 된다"는 구조적 제약이다. 그래서 실제로는
`DesktopPasteDelivery` 하나만 쓴다: 클립보드에 넣고 실제 Ctrl+V/Enter를 보내는, 사람이
직접 붙여넣는 것과 똑같은 방식. 이건 API가 아니라서 뺏을 락도, 충돌할 writer도 없다.

## Codex와 Claude Desktop의 능력은 비대칭이다

`DesktopPasteDelivery`는 두 타겟(`CODEX`/`CLAUDE`, `airpointer/desktop_paste.py`)을 같은
방식(UI 자동화 붙여넣기)으로 지원하지만, **둘이 그 다음에 할 수 있는 일은 다르다.** Codex는
셸 실행/로컬 파일 도구(`view_image` 등)가 기본 탑재돼 있는 반면, Claude Desktop은 사용자가
직접 MCP 커넥터를 붙이지 않는 한 그런 도구가 없다. 그래서 "붙여넣은 텍스트를 보고 AI가
스스로 뭔가 더 실행하게 하는" 기능(예: 매니페스트 기반 추가 프레임 조회)은 **Codex
전용**으로 게이팅한다(`self.codex.target is CODEX`) — Claude한테 같은 안내를 줘봤자 실행할
방법이 없는 죽은 텍스트이기 때문이다. 반대로 "그냥 텍스트/이미지를 붙여넣는" 기능(앵커
프레임, 활동 이력 힌트)은 붙여넣기 자체가 도구 의존적이지 않으므로 둘 다 동일하게 지원한다.

새 기능을 붙여넣기 파이프라인에 추가할 때마다 물어볼 질문: "이건 그냥 붙여넣는 텍스트/
이미지인가, 아니면 상대방이 뭔가 실행해야 하는 지시인가?" 전자는 두 타겟 다 되고, 후자는
Codex만 가능하다고 가정하고 시작할 것(실측 전까지).

## 알려진 중복과 기술 부채

- **변화 감지 알고리즘이 두 언어로 존재한다**: `airpointer/screen_buffer.py`(Python,
  `_ChangeTracker`)와 `web/src/lib/replay-buffer.ts`(TypeScript, `ChangeTracker`)가 거의
  같은 알고리즘(160×90 다운스케일, 전역/타일 임계값)을 각자 구현하고 있다. 웹 쪽은 브라우저
  자체 화면공유(설치 없는 데모)를 위해 필요해서 생긴 것이지, 실수로 중복된 게 아니다. 다만
  한쪽만 고치고 다른 쪽을 안 고치면 조용히 갈라진다. 현재 웹에도 MMR 다양성 선택이
  반영되어 있다(`selectEventsDiverse`). 이후 알고리즘 변경 시 양쪽을 확인한다.
- **매니페스트 기반 추가 프레임 조회는 웹에 먼저 있었다**: `web/scripts/replay-frame.mjs` +
  `route.ts`의 `frameQuery`가 원본이고, 네이티브 `airpointer/replay_query.py`는 그걸 그대로
  포팅한 것이다. 웹 쪽을 고칠 땐 네이티브도 같이 볼 것(그 반대도 마찬가지).
- **`CodexAppServerDelivery`(네이티브)는 죽은 코드에 가깝다**: 타입 힌트/시그니처엔 남아있지만
  실제로 인스턴스화하는 곳이 없다(위 "전송 메커니즘" 참고). 완전히 지울지, 아니면 나중에
  다른 통합 방식(예: AirPointer를 Codex Desktop의 자식 프로세스로 실행)을 위해 남겨둘지는
  아직 결정 안 됨.

## 아직 실측하지 않은 것

- Codex Desktop이 매니페스트의 `frameQuery` 안내를 보고 실제로 (승인 프롬프트 없이) 조용히
  명령을 실행할지는 그 세션의 승인 정책에 달려 있다 — 구조상 "가능"과 실사용성은 별개이며,
  아직 실제 Codex Desktop에 붙여넣어 확인해보지 않았다.
- 같은 매커니즘을 Claude Desktop에 적용할 수 있는지도(사용자가 파일/셸 계열 MCP를 직접
  붙여놓은 경우) 실측 전이다.
