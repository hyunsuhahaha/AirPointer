# Product

## Purpose

“방금그거뭐였지”는 사용자가 자기 상황을 AI에게 다시 설명하는 부담을 줄인다. 사용자가 공유한 직전 화면과 관측된 변화를 문서·이미지·녹화 파일로 묶어 기존 AI 에이전트에 전달한다.

## Competition scope

원티드 AI 공모전 제출용 핵심은 설치 없는 브라우저 체험이다. 방문자는 화면 공유를 허용하고 항상 위에 뜬 작은 창에서 Manual·Agent Link·Local Folder 중 하나를 고른다. 공유 전에는 실제 작업 녹화로 같은 흐름을 체험할 수 있다.

## Mechanism and limits

- 브라우저는 사용자가 고른 화면·창·탭만 공유 권한을 받은 뒤 기록한다.
- 최근 1·3·5분은 브라우저 메모리의 제한된 순환 버퍼에 남는다. 공유를 중지하면 버퍼를 비운다.
- Manual은 대표 화면을 먼저 보여주고 `…`로 사이 화면을 펼친다. 기본 선택은 0장이며 확대 보기, 브라우저 기본 이미지 드래그, 선택한 여러 장 다운로드를 제공한다. 갱신 버튼은 현재 시각 기준으로 목록을 다시 만든다.
- Agent Link는 `context.md`, `events.json`, 선별 이미지를 비공개 저장소에 올리고 20분 동안 유효한 토큰 URL을 만든다. 에이전트는 URL 하나로 HTML 타임라인과 원본 자료를 읽는다.
- Local Folder는 사용자가 허용한 폴더 아래에 `Context-*` 폴더를 만들고 문서·JSON·이미지를 저장한다. 프롬프트에는 에이전트가 찾을 실제 경로가 포함된다.
- Document PiP는 지원되는 데스크톱 브라우저에서 버퍼 길이·최근 구간·세 전달 모드·내보내기와 갱신을 제공한다.
- 화면 변화는 관측 자료다. 브라우저 화면 공유만으로 다른 앱의 실제 클릭·키 입력은 수집할 수 없다.
- Windows 앱은 설치한 사용자를 위한 전역 단축키 확장이다. 카메라와 손 인식은 제품에서 제거했다.
- Agent Link는 배포 환경에 Private Blob 설정이 필요하다. Local Folder에는 브라우저의 폴더 쓰기 권한이 필요하고, PiP와 외부 앱 드래그 동작은 브라우저 지원 범위에 따른다.

## Principles

- 사용자가 설명하기 전에 화면 맥락을 보여준다.
- 기록과 전송 상태를 사용자가 알 수 있게 한다.
- 화면 근거를 답변에 연결한다.
- 실측하지 않은 성능, 사용자 수, 성과를 주장하지 않는다.
- 핵심 동작은 버튼·키보드로 사용할 수 있게 한다.
