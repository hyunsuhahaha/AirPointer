# Product

## Purpose

“방금그거뭐였지”는 사용자가 자기 상황을 AI에게 다시 설명하는 부담을 줄인다. 사용자가 공유한 직전 화면과 관측된 변화를 문서·이미지·녹화 파일로 묶어 기존 AI 에이전트에 전달한다.

## Competition scope

원티드 AI 공모전 제출용 핵심은 설치 없는 브라우저 체험이다. 방문자는 화면 공유를 허용하고 항상 위에 뜬 작은 창에서 전체 접근·파일 첨부 중 하나를 고른다. 내보내기는 맥락 파일과 붙여넣을 프롬프트를 만든다. 공유 전에는 실제 작업 녹화로 같은 내보내기 흐름을 체험할 수 있다.

## Mechanism and limits

- 브라우저는 사용자가 고른 화면·창·탭만 공유 권한을 받은 뒤 기록한다.
- 최근 1·3·5분은 브라우저 메모리의 제한된 순환 버퍼에 남는다. 공유를 중지하면 버퍼를 비운다.
- 전체 접근 모드는 기록 탭의 `window.whatwasContext` 읽기 통로를 열어 기록 문서·화면 변화 JSON·전체 미리보기 이미지·WebM 영상을 에이전트가 직접 탐색하게 한다. 다운로드와 압축 해제가 없다.
- 파일 첨부 모드의 기본 형식은 사용자가 최초 한 번 정한 상위 폴더 아래에 `Export-*` 폴더를 만들고 맥락 문서와 원본 이미지 6~12장을 저장한다. PDF 한 개 내보내기는 선택 형식이다.
- Document PiP는 지원되는 데스크톱 브라우저에서 다른 작업 중에도 버퍼 길이·에이전트가 볼 최근 구간·모드 선택·내보내기·프롬프트 복사를 제공한다.
- 화면 변화는 관측 자료다. 브라우저 화면 공유만으로 다른 앱의 실제 클릭·키 입력은 수집할 수 없다.
- Windows 앱은 설치한 사용자를 위한 전역 단축키 확장이다. 카메라와 손 인식은 제품에서 제거했다.
- 전체 접근 모드는 기록 탭에서 JavaScript를 실행할 수 있는 에이전트에게 유효하며, 탭을 닫거나 공유를 멈추면 접근이 종료된다. 원본 파일 폴더에는 브라우저의 폴더 쓰기 권한이 필요하고, PiP 동작은 브라우저 지원 범위에 따른다.

## Principles

- 사용자가 설명하기 전에 화면 맥락을 보여준다.
- 기록과 전송 상태를 사용자가 알 수 있게 한다.
- 화면 근거를 답변에 연결한다.
- 실측하지 않은 성능, 사용자 수, 성과를 주장하지 않는다.
- 핵심 동작은 버튼·키보드로 사용할 수 있게 한다.
