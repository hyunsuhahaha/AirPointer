import type { ReactNode } from "react";
export const metadata = { title: "Developer Replay Source | 개발 작업 체험" };
export default function DeveloperLayout({ children }: { children: ReactNode }) { return children; }
