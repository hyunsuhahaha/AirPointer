# WhatWasThat 인수인계

## 현재 구조

- 공모전용 핵심 구현은 `web/`의 브라우저 앱이다. 네이티브 EXE는 현재 작업 범위가 아니다.
- 사용자가 브라우저 화면 공유를 시작하면 최근 1·3·5분을 로컬 순환 버퍼로 기록한다.
- 상단 PiP 내보내기 창에서 버퍼 길이와 전송 구간을 선택한다.
- 내보내기 모드는 다음 세 가지다.
  - `Manual`: 대표 화면과 대표 화면 사이의 화면을 직접 고른다. 기본 선택은 0장이다.
  - `Agent Link`: 컨텍스트 문서와 화면을 토큰 URL로 전달한다.
  - `Local Folder`: 사용자가 고른 로컬 폴더에 `Context-*` 폴더를 만든다.
- 순환 버퍼는 1초 단위 WebM 조각을 보관한다. 화면 변화 감지는 250ms 간격이고 저화질 미리보기는 1초 간격으로 저장한다.
- Manual 대표 화면은 WebM 조각에서 복원한다. 복원 동시 작업은 최대 2개다.
- `…`을 누르면 중간 화면을 즉시 펼치고, 보이는 화면부터 한 장씩 고화질로 복원한다.
- 이미지 전달은 브라우저의 기본 `<img>` 드래그를 사용한다. 별도 드래그 데이터나 `DownloadURL`은 사용하지 않는다.

## 최근 변경

### `0a19a09` — Manual 이미지 품질 상향

- WebM 비트레이트를 2Mbps에서 4Mbps로 올렸다.
- Manual 복원의 1600px 폭 제한을 제거하고 공유 원본 해상도를 사용한다.
- 복원 JPEG 품질을 0.94로 올렸다.
- Manual 영역 선택 결과는 JPEG 품질 0.95를 사용한다.

### `a247559` — 사진별 영역 선택

- Manual에서 사진을 확대하면 상단에 `영역 선택` 버튼이 표시된다.
- 사진 위에서 드래그한 영역만 새 JPEG로 만든다.
- 잘린 사진은 기존 방식 그대로 드래그하거나 다운로드할 수 있다.
- `선택 취소`를 누르면 해당 사진의 원본으로 돌아간다.
- 영역 선택은 다른 사진에 영향을 주지 않는다.

### `13a350c` — 중간 화면 상태 표시

- 접힌 상태는 `…`으로 표시한다.
- 첫 고화질 화면을 준비하는 동안 회전 아이콘을 표시한다.
- 펼쳐진 뒤에는 접기 화살표를 표시한다.

### `efed5d8` — 브라우저 부하 감소

- 저화질 미리보기 저장과 배열 정리를 1초 간격으로 줄였다.
- 고화질 복원 동시 작업을 2개로 제한했다.
- 중간 화면 고화질 캐시는 최대 24장이다.
- PiP CSS 전체 동기화는 개발 환경에서 2초 간격이며 배포 환경에서는 반복 실행하지 않는다.

## 현재 작업 트리

- `main`의 원격 최신 커밋은 `0a19a09`다.
- 타임라인 눈금과 확대 보기 이전·다음 이동 작업이 별도 미커밋 변경으로 남아 있다.
- 위 미커밋 변경은 최근 품질·영역 선택 커밋과 분리되어 있다.
